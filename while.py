# A una variable le asignamos por ejemplo un numero
numero = 6
# permitira ingresar al bucle siempre y cuando el numero sea mayor a 1
while numero > 1:
    numero = (
        numero - 1
    )  # A la variable numero se resta uno porque sino el ciclo seria infinito)
    print(numero)
